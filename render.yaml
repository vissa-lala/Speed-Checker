services:
  - type: web
    name: neon-speed-dashboard
    env: node
    buildCommand: bash .render/build.sh
    startCommand: serve -s build -l 3000
    envVars:
      - key: NODE_ENV
        value: production
