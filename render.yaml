services:
  - type: web
    name: speed-checker
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: |
      cd ../frontend && npm install && npm run build && cd ../backend && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
