services:
  - type: web
    name: neon-speed-dashboard
    env: node
    buildCommand: npm install && npm run build
    startCommand: serve -s build -l 3000
    envVars:
      - key: NODE_ENV
        value: production
