services:
  - type: web
    name: speed-checker
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s build -l 3000
    routes:
      - path: /*
        destination: /index.html


